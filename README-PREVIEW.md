# Fast preview (no Turbopack)

**Build is ~6s. Preview is instant.**

```bash
cd ~/Documents/peer-csr-workflow-rail-dressup-2026-05-19
npm run preview
```

Open: http://localhost:3072/documents/abc877-201?tab=copilot

**Hot reload (webpack, not Turbopack):**

```bash
npm run dev
```

If port busy: `kill -9 $(lsof -ti:3072)` then retry.

**Demo path:** Copilot → Open workflow → Collect package → Review quorum → version chip → drift banner.
